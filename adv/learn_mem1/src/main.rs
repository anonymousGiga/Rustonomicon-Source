fn main() {
	//let i: i32; //为初始化，里边的内容你不知道是什么
	////let _j: i32 = i;//在Rust中，没有初始化的变量，是不允许读取的，此处i未被初始化，因此是不能直接使用的

	//if true {
	//	i = 1;
	//	println!("i = {}", i);
	//}

	////println!("i = {}", i); //编译器没有这么智能，它分析出来还有其它分支的情况没有给i初始化


	//----------------------------------------------
	let x: i32;
	loop {
		if true {
			x = 0;
			break;
		}
	}

	println!("x = {}", x); //编译器分析出来，跳到此处的条件只能通过if，而在if语句中是对x初始化过的
}


//c语言中：
//void main() {
//	int a;
//	int b = a;
//	printf("b = %d\n", b);
//}
